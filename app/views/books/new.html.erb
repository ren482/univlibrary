

<%= form_with(model: @book, local: true) do |f| %>

<%= f.label :title, "本のタイトル" %>
<%= f.text_field :title %>
<%= f.label :genre, "本のジャンル" %>
<%= f.text_field :genre %>
<%= f.select :status, {"既読":2, "未読":1}, { include_blank: "読み終えていますか？"} %>

<%= f.submit "内容を確認する" %>
<% end %>

