

<% if current_user.added?(@book) %>



<% else %>

<%= form_with(model: current_user.mybooks.build, local: true) do |f| %>

      
<%= hidden_field_tag :book_id, @book.id %>

      
<%= f.submit 'この内容で本棚に追加する！', class: 'btn btn-primary btn-sm' %>

<% end %>

<% end %>